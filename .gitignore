# 运行时数据与密钥 —— 永不入库
data/
.env
*.db
*.db-journal
*.db-wal
__pycache__/
*.egg-info/
dist/
build/
.DS_Store

# U 第二大脑 / 记忆数据 —— 只发布"记忆系统"(crewos/umemory.py 等代码),绝不上传主人的记忆内容。
# U 的真实数据在仓库之外(~/.openclaw、~/wiki),天然不会进库;下面用「根锚定」兜底:
# 万一有人把工作区设成仓库根(crewos start --workspace .),这些运行时产物也不会被误提交。
# 注意:用前导 / 只匹配仓库根,绝不波及随包发布的 crewos/templates/memory 等模板。
/memory/
/deliverables/
/agents/
/config/
.openclaw/
*.local.md
