# 复制为 .env 并填入真实 key,然后 source .env(或用 direnv)
# 铁律:key 只活在环境变量,永远不进 agent 上下文、不进任何配置文件
export OPENROUTER_KEY=""      # 一个 key 覆盖大部分模型(推荐先只配这个)
export OPENAI_KEY=""          # Codex 直连备用通道 (coder)
export DASHSCOPE_KEY=""       # Qwen 直连备用通道 (writer)
export MOONSHOT_KEY=""        # Kimi 直连备用通道 (researcher)
export DEEPSEEK_KEY=""        # DeepSeek 直连备用通道 (analyst/runner)
export ZHIPU_KEY=""           # 智谱直连备用通道 (builder)
export ARK_KEY=""             # 火山方舟,豆包视频理解 (perceiver,唯一通道必配)
